#include "questionbank.h"
#include <QRandomGenerator>

static const QVector<Question>& bank() {
    static const QVector<Question> q = {
        {"下列关于运算符重载的描述中,正确的是:",
         "如果在某个类中使用成员函数对运算符重载，其左操作数必须是该类的对象",
         "一元运算符只能作为成员函数重载",
         "二元运算符重载为非成员函数时,参数表中只有一个参数",
         "被重载的运算符的操作数可以都不是对象",
         0},
        {"下列关于类和对象，说法错误的是：",
         "静态成员函数内部不能访问同类的非静态成员变量, 也不能调用同类的非静态成员函数。",
         "静态成员函数中可以使用this指针。",
         "一个类可以有多个构造函数，但最多只能有一个析构函数。",
         "封闭类对象生成时，先执行所有成员对象的构造函数，再执行封闭类的构造函数。",
         1},
        {"下列关于运算符重载的说法中，正确的是：",
         "可以通过运算符重载来创建C++中原来没有的运算符。",
         "重载时，前置形式的自增运算符比后置形式的自增运算符多了一个int类型的参数。",
         "重载运算符应该作用于对象，不允许操作数都不是对象。",
         "运算符函数的返回类型不能声明为基本数据类型",
         0},
        {"以下四个说法，正确的是：",
         "对于一个含有静态变量的类，只要没有这个类型的对象，静态变量就不存在。",
         "用 struct 定义类和用 class 定义类的唯一区别就是：未说明是公有还是私有的成员，就是公有。",
         "构造函数和析构函数中不可以使用this指针。",
         "编译器总是会给每个类生成一个默认的无参数的构造函数。",
         1},
        {"给定如下代码片段：\\n```cpp\\n#include<cstdio>\\nusing namespace std;\\nclass A{\\npublic:\\nint a;\\n};\\nclass B : public A {\\npublic:\\nint b;\\nB(const B& x){\\nb = x.b;\\n}\\n}\\n```\\n运行如下语句或调用如下函数，会触发 A 的复制构造函数的是：",
         "void f1(B x){ }",
         "void f2(const B &x){ }",
         "A x; B y; x = y;",
         "B y; A x(y);",
         0},
        {"以下关于运算符重载的叙述，错误的个数是：\\n```\\n(1) 只能对已有运算符进行重载，不能通过运算符重载定义原本没有的新运算符\\n(2) 被重载的运算符的操作数可以都不是对象\\n(3) 运算符可以根据参数类型的不同进行多次重载\\n(4) 有些运算符只能重载为成员函数\\n```",
         "1",
         "2",
         "3",
         "4",
         0},
        {"以下关于静态成员的叙述，正确的有：\\n```\\n(1) 静态成员可以使用“指针->成员名”的方式访问\\n(2) 静态成员函数内部可以使用 this指针\\n(3) 静态成员变量可以被定义为private\\n(4) 同类的不同对象的静态成员变量可以有不同值\\n```",
         "(1)(2)",
         "(1)(3)",
         "(2)(4)",
         "(3)(4)",
         1},
        {"以下关于构造函数和析构函数的叙述，错误的是：",
         "构造函数可以是private的。",
         "一个类只能有一个析构函数。",
         "构造函数不可以是虚函数。",
         "析构函数不可以是纯虚函数。",
         3},
        {"以下说法正确的是：",
         "构造函数必须返回当前对象",
         "常量成员函数不能修改任何成员变量，包括静态成员变量",
         "重载赋值运算符“=”可以重载为全局函数",
         "()运算符不能重载为友元函数。",
         3},
        {"下列哪种情况运算符不能重载为类的成员函数：",
         "重载 << 用于将对象通过std::cout输出",
         "重载相等运算符==",
         "重载括号运算符()",
         "重载运算符[]",
         0},
        {"在能够正确编译的情况下，以下有几种情况属于成员函数的重载?\\n```\\n① 基类和派生类具有同名同参数表的函数\\n② 一个类的两个成员函数名字和返回值相同\\n③ 一个类的两个成员函数名字和参数表都相同，但返回值不同\\n④ 一个类的两个成员函数名字和参数表都相同，但一个是const，另外一个不是\\n```",
         "1",
         "2",
         "3",
         "4",
         1},
        {"以下关于封闭类的说法哪个是正确的?",
         "定义封闭类时一定要写构造函数",
         "定义封闭类时一定要写析构函数",
         "封闭类对象初始化时，成员对象都是用默认构造函数初始化的",
         "封闭类对象初始化时，成员对象的构造函数先于封闭类自身的构造函数被执行",
         3},
        {"下面关于类和对象的说法错误的是",
         "类的构造函数不能被定义为private的",
         "如果定义类时没写构造函数，编译器生成一个默认无参数的构造函数",
         "如果定义的自己的复制构造函数, 则默认的复制构造函数不存在",
         "一个类最多只能有一个析构函数，但是可以有多个构造函数",
         0},
        {"要求只能用成员函数重载的运算符是",
         "=",
         "==",
         "<=",
         "++",
         0},
        {"关于运算符重载说明，下面四个选项中哪个是正确的",
         "运算符函数作为成员函数重载时，最左边操作数必须是运算符的一个类对象（或者是对该类对象的引用）",
         "流输入输出运算符<<、>>一般作为成员函数重载",
         "当重载了+、=运算符时，则+=运算符会自动重载",
         "重载二元运算符时，必须声明两个形参",
         0},
        {"设X是一个类，现在想重载X的赋值运算符“=”，则其返回值最好是：",
         "void",
         "X*",
         "X",
         "X&",
         3},
        {"如下代码：\\n```cpp\\nclass A{\\n};\\nconst A func(A &c){\\nreturn c;\\n}\\nint main(){\\nA a, b = a;//1\\na = func(b);//return:1\\nreturn 0;\\n}\\n```\\n请问上面这段程序中，如果不考虑优化，调用了几次A的复制构造函数:",
         "0次",
         "1次",
         "2次",
         "3次",
         2},
        {"下面程序执行了几次A的析构函数？\\n```cpp\\nclass A\\n{\\npublic:\\n~A() { }\\n};\\nint main()\\n{\\nA * p = new A[2];\\nA * a = new A();\\nA aa;\\ndelete a ;\\nreturn 0;\\n}\\n```",
         "1",
         "2",
         "3",
         "4",
         2},
        {"以下的运算符中哪个不能重载",
         "?:",
         "++",
         "<<=",
         "==",
         0},
        {"如下类定义中有多少条是错误的：\\n```cpp\\nclass Foo1 {public int x;};\\nclass Foo2 {static int x;};\\nclass Foo3 {const int x;};\\nclass Foo5 {virtual int x;};\\n```",
         "0",
         "1",
         "2",
         "3",
         2},
        {"以下关于C++析构函数的叙述，正确的有：\\n```\\n(1) 析构函数可以被重载\\n(2) 析构函数可以被声明为虚函数\\n(3) 派生类的析构函数先于基类的析构函数被调用\\n(4) 如果一个类定义了析构函数，它就不能生成默认的构造函数\\n```",
         "(1)(3)",
         "(2)(3)",
         "(2)(4)",
         "(1)(4)",
         1},
        {"给定如下代码片段：\\n```cpp\\n#include<cstdio>\\nusing namespace std;\\nclass A{\\npublic:\\nint a;\\n};\\nclass B : public A {\\npublic:\\nint b;\\nB(const B& x){\\nb = x.b;\\n}\\n}\\n```\\n运行如下语句或调用如下函数，会触发 A 的复制构造函数的是：",
         "void f1(B x){ }",
         "void f2(const B &x){ }",
         "A x; B y; x = y;",
         "B y; A x(y);",
         0},
        {"派生类的成员函数，可以访问以下哪几种成员变量？\\n```\\n(1) 当前基类对象的private成员变量\\n(2) 当前基类对象的protected成员变量\\n(3) 其他基类对象的protected成员变量\\n(4) 其他同类对象的protected成员变量\\n```\\n（当前对象是指成员函数所作用的那个对象，亦即成员函数的隐藏this指针指向的对象）",
         "1, 2, 3",
         "1, 2",
         "2, 4",
         "2",
         2},
        {"下列关于基类和派生类实现的说法中，正确的有：\\n```\\n(1) 若想实现虚函数，基类中的virtual关键字可以省略，但派生类中的virtual关键字不可省略；\\n(2) 对于基类中虚函数func()，若派生类未重新实现该虚函数(即直接继承)，在派生类和基类的虚函数表中，该虚函数的地址相同；\\n(3) 对于基类中虚函数func()，若派生类重新实现该虚函数，在派生类和基类的虚函数表中，该虚函数的地址不同；\\n(4) 若派生类对基类的继承不是public继承，则不可将派生类对象地址直接赋值给基类指针。\\n```",
         "1, 2, 3",
         "3, 4",
         "2, 3, 4",
         "2, 3",
         2},
        {"下面有几种情况的函数调用语句是多态？\\n```\\n1) 通过基类指针调用基类和派生类中都有的同名同参数表的虚函数\\n2) 通过基类引用调用基类和派生类中都有的同名同参数表的虚函数\\n3) 在构造函数中调用本对象的虚函数\\n4) 在const成员函数中调用虚函数\\n```",
         "1",
         "2",
         "3",
         "4",
         2},
        {"以下关于基类和派生类的成员函数的叙述，正确的是(其它对象指的是并非this指针指向的对象)：",
         "派生类的成员函数可以访问继承自基类的private成员变量",
         "派生类的成员函数可以访问其他基类对象的protected成员变量",
         "派生类的成员函数可以访问其他同类对象的private成员变量",
         "基类的成员函数可以访问派生类的protected成员变量",
         1},
        {"以下关于虚函数的叙述，正确的个数是：\\n```\\n(1) 静态成员函数可以是虚函数\\n(2) 友元函数不可是虚函数\\n(3) 如果一个基类含有虚函数，则在它的派生类中，与该虚函数的函数名、参数个数和参数列表均相同的函数也是虚函数。\\n(4) 如果一个基类含有纯虚函数，则它的派生类不允许再含有纯虚函数\\n```",
         "1",
         "2",
         "3",
         "4",
         1},
        {"下面关于多态的说法正确的是",
         "静态成员函数也可以用virtual修饰",
         "基类指针指向派生类对象时，通过该指针调用基类和派生类中的同名同参数表函数，则调用的是派生类中的函数。",
         "在构造函数和析构函数中调用虚函数也可以是多态。",
         "写虚函数时，virtual关键字只用在类定义的函数声明中，写函数体时不用",
         3},
        {"假设在一个基类和一个派生于该基类的派生类中分别定义了一个同名同参数表的虚函数，\\n那么以下情况一定不可能发生的是",
         "使用基类指针调用该虚函数，实际调用了在基类中定义的虚函数；",
         "使用基类指针调用该虚函数，实际调用了在派生类中定义的虚函数；",
         "使用派生类指针调用该虚函数，实际调用了在基类中定义的虚函数；",
         "使用派生类指针调用该虚函数，实际调用了在派生类中定义的虚函数；",
         2},
        {"下面关于虚函数的说法正确的是",
         "静态成员函数也可以用virtual修饰",
         "在构造函数和析构函数内部可以调用纯虚函数",
         "析构函数可以是虚函数",
         "通过\"基类指针->函数名()\"无法调用派生类中的私有虚函数",
         2},
        {"下列说法错误的是",
         "抽象类既可以派生出抽象类，也可以派生出非抽象类",
         "大多数情况下纯虚函数可以没有函数体",
         "凡是包含虚函数的类都是抽象类",
         "抽象类的构造函数或析构函数内部不能调用自己的纯虚函数",
         2},
        {"关于 virtual 修饰符, 下面说法正确的是:",
         "使用 virtual 修饰符修饰成员函数，不会导致类的对象需要更大的内存空间",
         "静态成员函数也可以用virtual修饰",
         "如果基类的析构函数不是 virtual 的, 则delete一个指向派生类对象的基类指针，不会执行派生类的析构函数",
         "没有用virtual修饰的成员函数，一定不是虚函数",
         2},
        {"在C++的输入输出及文件操作机制中，哪个说法是正确的？",
         "打开文件时，如果文件不存在，系统会自动创建一个新文件。",
         "可以用 ios::binary 模式打开文本文件进行读写",
         "cin和cout是预定义的流对象，分别用于从标准输入读取和向标准输出写入数据，它们都是文件流对象。",
         "istream和ostream是基类，cin和cout是它们的派生类对象。",
         1},
        {"有关函数模板和模板函数说法错误的是:",
         "函数模板只是对函数的描述，编译器不为其产生任何执行代码",
         "模板函数可以由编译系统在遇到具体函数调用时所生成，并调用执行",
         "函数模板需要实例化为模板函数后才能执行",
         "当函数模板和一般函数同名时，系统先去匹配函数模板，将其实例化后进行调用",
         3},
        {"有如下定义：\\n```cpp\\ntemplate < class T1,class T2,class T3 >\\nT1 func(T1 *a, T2 b, T3 c){}\\nchar a[8], b[8];\\nvoid f(char* n){}\\n```\\n则func(a,b,f); 将模版类型参数实例化的结果是：",
         "T1: char, T2: char*, T3: void(char*)",
         "T1: char*, T2: char, T3: void(char*)",
         "T1: char, T2: char*, T3: void(*)(char*)",
         "T1: char*, T2: char, T3: void(*)(char*)",
         2},
        {"下列哪个选项描述了类模板的特点？",
         "类模板不可以定义静态成员变量。",
         "类模板必须有默认构造函数。",
         "类模板可以指定非类型模板参数",
         "类模板不能被继承和重载。",
         2},
        {"以下哪个说法是正确的？",
         "类模板可以有多个类型参数，函数模板只能有一个类型参数。",
         "类模板可以有默认的类型参数，函数模板不能有默认的类型参数。",
         "函数模板可以有多个非类型参数，类模板只能有一个非类型参数。",
         "函数模板可以被重载，而两个类模板名字不能相同。",
         3},
        {"下列四句话中正确的有多少个 ?\\n```\\n1. 函数模板中可以有不止一个类型参数\\n2. 编译器必须通过函数模板调用语句中的实参类型，来实例化函数模板的中的类型参数\\n3. 在多个函数和函数模板名字相同的情况下，编译器最优先选择完全匹配的普通函数，最后匹配经过自动类型转换后可以匹配的模板函数。\\n4. 不允许用模板的类型参数表示函数模板的返回值类型\\n```",
         "1",
         "2",
         "3",
         "4",
         1},
        {"下列关于类模板的说法正确的有几个\\n```\\n1. 类模板的<类型参数表>中可以出现非类型参数\\n2. 函数模板不能作为类模板的成员出现\\n3. 类模板不可以从模板类中派生，但是类模板可以从类模板中派生。\\n4. 类模板中可以定义友元函数f，那么从该类模板实例化得到的所有类，都将f视为友元。\\n```",
         "1个",
         "2个",
         "3个",
         "4个",
         1},
        {"下列四句话中正确的有___个。\\n```\\n1.函数模板中可以有不⽌⼀个类型参数。\\n2.编译器必须通过函数模板调用语句中的实参类型，来实例化函数模板的中的类型参数\\n3. 不可以定义和函数模板同名的函数。\\n4. 函数模板中的类型参数可以⽤来表示函数模板的返回值类型。\\n```",
         "1",
         "2",
         "3",
         "4",
         1},
        {"以下关于STL的叙述，错误的个数是：\\n```\\nqueue支持双向访问迭代器\\nset的insert成员函数返回值是被插入元素的迭代器 pair<iterator,bool>\\n所有适用于vector的算法（STL的函数模板）都适用于deque\\n不可以对multiset中的元素进行修改，只能添加或者删除\\n```",
         "1",
         "2",
         "3",
         "4",
         1},
        {"以下有关STL的叙述，错误的是：",
         "vector本质上是一个动态数组，在尾部添加速度很快，但向中间插入较慢。",
         "list在任何位置插入/删除都是常数时间，支持随机存取。",
         "deque是一个顺序容器，元素的插入位置和元素的值无关。",
         "map同multimap的不同在于是否允许相同key的元素出现。",
         1},
        {"以下STL常见容器的用法，一定不会导致错误的是：",
         "Alice同学希望创建一个priority_queue用来维护自定义类A的对象，为此她细心地给没有重载任何运算符的类A重载了>运算符。",
         "Bob同学希望创建一个map用来实现int到vector<int>的映射，但是他忘记了重载运算符来比较两个vector。",
         "Carol同学希望在一个排好序的vector(记作vv)中查找第一个大于等于x的数字（如果不存在输出-1），她不想写太多代码，于是直接输出了*lower_bound(vv.begin(),vv.end(),x)。",
         "Dave同学希望将一个list(记作lst)从小到大排序，刚学会std::sort的他不假思索地写下了std::sort(lst.begin(),lst.end())。",
         1},
        {"以下四个说法，正确的是：",
         "对于set<int>s;，可通过s.end()-s.begin()计算s中的元素个数。",
         "对于deque<vector<int> >d;可通过sort(d.begin(),d.end())进行排序。",
         "对于multimap<int,double>m，可通过m[3]访问key 3 对应的value。",
         "对于int a[]={1,3,2};，调用next_permutation(a,a+3)会返回false.",
         3},
        {"给定如下 stl 容器 T，其包含迭代器 T::iterator a，但是其迭代器 T::iterator a 不支持 a = a + 10 操作。则 T 可以是：",
         "set",
         "queue",
         "deque",
         "Stack",
         2},
        {"以下STL中的函数模板哪个可以作用于vector",
         "lower_bound",
         "random_shuffle",
         "unique",
         "都可以",
         3},
        {"以下STL容器中，支持随机访问迭代器的是：",
         "set",
         "list",
         "deque",
         "Multimap",
         2},
        {"小明定义了\\n```cpp\\nqueue<int>q;\\n```\\n下面的说法中正确的是：",
         "可以用迭代器访问q中的元素",
         "可以用sort算法对q进行排序",
         "可以使用q.push_front (1)在队头插入整数1",
         "q是先进先出的",
         3},
        {"以下说法正确的是：?",
         "对于set<int>s;，可通过s.end()-s.begin()计算s中的元素个数。",
         "对于deque<vector<int> >d;可通过sort(d.begin(),d.end())进行排序。",
         "对于multimap<int,double>m，可通过m[3]访问key 3 对应的value。",
         "对于int a[]={1,3,2};，调用next_permutation(a,a+3)会返回false.",
         1},
        {"下面关于容器的说法中，不正确的有哪些\\n```\\n①map和multimap都是关联容器，其中multimap根据first值从小到大对元素排序，当出现first值相同的元素时，根据second值从小到大对元素排序\\n②可以对priority_queue进行top操作，操作返回的是最大元素的引用，可以方便地对其进行读取和更改\\n③list容器不支持STL的算法sort，可以使用成员函数sort进行排序，这里的排序默认是将元素从小到大排序的\\n④当需要找出一个vector<int> v中与1相等的元素时，可以使用STL中的find算法，具体用法是用find(v.begin(), v.end(), 1)获得指向该元素的迭代器\\n```",
         "①②",
         "①④",
         "②③",
         "③④",
         0},
        {"STL的算法find可以用于以下几种容器或容器适配器？\\n```\\n① set   ② vector    ③ list    ④ stack\\n```",
         "1",
         "2",
         "3",
         "4",
         2},
        {"下列说法错误的是",
         "不可以用随机访问迭代器访问list容器",
         "可以用随机访问迭代器访问deque容器",
         "容器适配器priority_queue默认的元素比较器是less<T>",
         "unique算法会修改vector的大小",
         3},
        {"下面哪个STL函数可以被用于关联容器set？",
         "Find",
         "random_shuffle",
         "Sort",
         "以上都不可以",
         0},
        {"下列说法正确的是",
         "vector容器可以使用\"[]\"访问，在vector的头部和尾部插入元素是常数时间。",
         "list容器元素在内存中是连续存储的，在list的任何位置插入删除元素都是常数时间。",
         "不可以用标准库的sort函数对list容器排序。",
         "deque容器不可以使用\"[]\"访问",
         2},
        {"假设有 std::string s，以下表达式中，不能用==运算符比较的是：",
         "s.data() 和 s.c_str()",
         "s[1] 和 s.at(1)",
         "s.find('a') 和 std::find(s.begin(), s.end(), 'a')",
         "s.capacity() 和 s.empty()",
         2},
        {"下列关于 C++ STL 的说法中，正确的一项是：",
         "q.top() 返回 priority_queue q 中元素的最小值",
         "*s.end() 返回 set s 中元素的最大值",
         "m.rbegin()->second 返回 map<int, int> m 中关键字最大的元素的值",
         "v.insert(v.begin(), 5) 和 q.push_front(5) 可以实现在 vector v 和 deque q 的头部插入元素 5，并且它们的时间复杂度也是相同的",
         2},
        {"i1,i2是某STL容器上X的迭代器，如下代码能通过编译，则X可能是：\\n```cpp\\nprintf(\"%d\\n\",int(i1-i2));\\n```",
         "queue",
         "stack",
         "vector",
         "Map",
         2},
        {"下面哪个操作可以将 bitset<114> x; 设置为全 0 bitset?",
         "x.clear();",
         "x.reset();",
         "x.flip();",
         "x.none();",
         1},
        {"以下支持[]运算符的类模板共多少个？\\n```\\nvector,deque,list,set,multiset,map,multimap,stack,queue,priority_queue\\n```",
         "3",
         "4",
         "5",
         "6",
         0},
        {"以下正确的是：",
         "一个最小值优先的优先队列类型定义为priority_queue < int, greater < int> >",
         "end()返回指向容器中最后一个元素的迭代器",
         "在vector里面插入新元素一定是常数时间",
         "unique算法不会修改vector的大小",
         3},
        {"关于lambda表达式，以下哪个描述是错误的？",
         "可以将lambda表达式赋值给一个变量。",
         "在lambda表达式中只能读取，不能修改在该表达式外部定义的变量。",
         "lambda表达式可以没有参数，也可以有任意数量和类型的参数。",
         "lambda表达式可以没有返回值",
         1},
        {"以下关于C++ 11特性说法中，正确的有哪些\\n```\\n①类型为auto的变量在定义时必须初始化\\n②智能指针shared_ptr对象可以托管一个new运算符返回的指针，也可以正确托管指向动态分配的数组的指针，系统会为原始的指针维护一个托管计数\\n③右值引用和move的主要目的是提高程序运行的效率，减少需要进行深拷贝的对象进行深拷贝的次数，左值可以用来初始化右值引用，右值不能用来初始化左值引用。\\n④无序容器unordered_map又称哈希表；和map、multimap不同，unordered_map内部的元素是无序的，但是插入和查询的时间复杂度几乎是常数\\n```",
         "①②",
         "①④",
         "②③",
         "③④",
         1},
        {"以下关于lambda表达式的说法有几项正确？\\n```\\n① lambda表达式无法递归调用自身\\n② lambda表达式中不可修改其外部的变量的值\\n③ 可以用lambada表达式对变量进行赋值\\n④ lambda表达式可以作为类的成员函数\\n```",
         "1",
         "2",
         "3",
         "4",
         1},
        {"下面的说法有几个是正确的?\\n```\\n1. shared_ptr模板实例化出来的智能指针对象在消亡的时候会delete 其托管的指针\\n2. 可以用变量初始化右值引用\\n3. lambda 表达式也可以递归调用自身\\n4. 如果没写移动构造函数，编译器会自动生成一个\\n```",
         "1",
         "2",
         "3",
         "4",
         1},
        {"下列说法错误的是：",
         "右值引用能够避免不需要的深拷贝。",
         "智能指针shared_ptr对象需要使用reset方法托管指向动态分配的数组的指针。",
         "无序容器的优势是查询和插入的速度快，但是无法按照键值大小顺序访问。",
         "Lambda表达式中如果没有用->指定返回值类型，则编译器自动判断返回值类型",
         1},
        {"关于C++中的指针和引用，以下哪些说法是正确的？\\n```\\n(1) 指针和引用在本质上是一样的，都是变量的别名。\\n(2) 指针可以值为NULL，但引用必须始终引用一个变量。\\n(3) 指针可以被重新赋值以指向另一个对象，但引用一旦初始化后就不能将其改为引用别的变量。\\n(4) const指针指向的对象和const引用引用的对象的值都不可以被修改。\\n```",
         "(1)(2)(3)",
         "(1)(2)(3)(4)",
         "(1)(4)",
         "(2)(3)",
         3},
        {"以下说法正确的有（）个：\\n```\\n1. 可以利用istream_iterator和ostream_iterator进行输入和输出\\n2. 小明定义了class A以及对应的对象a。为了能够实现++a，小明需要定义成员函数 A operator ++ (int){...};\\n3. 对于string类的数组，可以用memcpy进行正确复制。\\n4. 对于string类的对象s，可以这样进行赋值： s = \"Are you OK?\";\\n```",
         "3",
         "2",
         "1",
         "0",
         3},
        {"系统在调用重载函数时，往往根据一些条件确定哪个重载函数被调用，在下列选项中，不能作为依据的是_____。",
         "参数个数",
         "参数的类型",
         "函数的返回值的类型",
         "函数名称",
         2},
        {"以下函数或类使用时一定不会出现二义性的有：",
         "void x(int y); void x(int p, int q=0);",
         "class x{public: int p;}; class y{public:int p;}; class z: public x, public y{};",
         "template<class Foo> int print(Foo x); template<class Bar> void print(Bar y);",
         "template<class Foo> int print(Foo x); void print(int y);",
         1},
        {"以下语法正确的是：",
         "void foo(int a=0, int b){}",
         "int arr[]{1,2,3};",
         "int & r = 2+3;",
         "template < class T1, T2>",
         1},
    };
    return q;
}

Question QuestionBank::drawRandom() {
    const auto& b = bank();
    return b[QRandomGenerator::global()->bounded(b.size())];
}

int QuestionBank::size() {
    return bank().size();
}