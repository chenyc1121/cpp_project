# 程设大富翁 mOnOPoly — 路演视频素材大纲与分镜

> 目标时长：5-7 分钟  
> 录制工具建议：OBS / 屏幕录制 + 画外音  
> 分辨率：1920×1080，游戏窗口建议 960×800 或全屏

---

## 分镜总览

| 镜号 | 内容 | 时长 | 核心展示点 |
|:---:|------|:---:|----------|
| 1 | 开场 & 项目定位 | 30s | 北大校园主题 + OOP 教学游戏 |
| 2 | 启动游戏 & UI 概览 | 45s | Qt 界面、地图、状态栏、玩家面板 |
| 3 | 基础玩法：掷骰 → 移动 → 购地 → 收租 | 60s | 状态机驱动回合、地产系统 |
| 4 | 特色机制①：虚函数格 & 虚函数卡 | 60s | 多态机制融入游戏、基类/派生类选择 |
| 5 | 特色机制②：迭代器格 & 迭代器卡 | 50s | STL 迭代器概念、3 种子类型、vector 顺序传送 |
| 6 | 特色机制③：静态变量格 + 问答/上机课 | 50s | 静态成员变量集齐加成、50 题 C++ 真题 |
| 7 | 特色机制④：商店 & 效果卡系统 | 45s | 5 种卡牌策略、29 楼地下室传送 |
| 8 | 代码架构讲解（IDE/编辑器画面） | 90s | Tile 多态继承、Game 状态机、信号/槽、config 配置化棋盘 |
| 9 | 收尾 & 亮点总结 | 30s | 寓教于乐、架构可扩展 |

---

## 详细分镜脚本

### 镜号 1：开场 & 项目定位（30s）

**画面**：黑色背景 + 项目 Logo/标题淡入，随后切换到 README 或代码编辑器展示项目信息。

**画外音/字幕**：
> "大家好，这是我们团队开发的《程设大富翁 mOnOPoly》—— 一款以北大校园为地图、将 C++ 面向对象核心概念融入游戏机制的大富翁类游戏。"

**录制操作**：
1. 展示 README.md 开头部分（项目标题、快速开始）。
2. 快速闪过一行：`C++17 + Qt6 Widgets，CMake 构建，信号/槽驱动架构`。

**要点**：让观众 10 秒内明白——这是大富翁，但和 OOP 课程强相关。

---

### 镜号 2：启动游戏 & UI 概览（45s）

**画面**：程序运行窗口，棋盘清晰可见。

**录制操作**：
1. 启动游戏执行文件，进入主界面。
2. 点击菜单 **游戏 → 新游戏**，弹出对话框选择 4 人（或 2 人）。
3. 进入主界面后，鼠标缓慢扫过：
   - 左侧 8×8 棋盘（28 格，4 角 + 每边 6 格）
   - 右上方状态栏（黄色提示区）
   - 右侧玩家信息面板（资金、颜色标识）
   - 骰子区域 + 事件日志区
   - 底部玩家道具栏

**画外音**：
> "游戏基于 Qt6 Widgets 开发，采用 8×8 棋盘布局。界面分为棋盘区、玩家面板、骰子与事件日志四大模块，所有交互通过信号/槽驱动。"

---

### 镜号 3：基础玩法演示（60s）

**画面**：完整的一到两个回合，展示标准大富翁流程。

**录制操作**：
1. 玩家 A 回合，点击**掷骰**（或开启 DEBUG 模式输入固定点数如 3+4=7）。
2. 展示玩家棋子沿棋盘移动动画。
3. 落点为无主地产（如"45甲"），弹出购买对话框 → 点击**购买**。
4. 玩家 B 回合，掷骰移动到玩家 A 的地块 → 自动支付租金，事件日志显示扣款信息。
5. 玩家 A 再次踩中自己的地，展示**建房**对话框（如果已集齐同色组），点击建造 1 栋房。
6. 鼠标悬停在格子上，展示 tooltip（价格、租金表、所有者）。

**画外音**：
> "核心玩法与传统大富翁一致：掷骰前进，购买地产，收取租金，建造房屋。但我们用 C++ 多态实现了 11 种地块类型，每种地块的 landOn 行为各不相同。"

**代码插入（可选，5s 闪切）**：
- 展示 `tile.h` 中 `Tile` 基类与 `PropertyTile::landOn` 的虚函数声明。

---

### 镜号 4：特色机制① — 虚函数格 & 虚函数卡（60s）

**画面**：聚焦虚函数格（如 Buy / Pure / Rent / Mix1 / Mix2），展示 [i] 按钮弹出的伪代码。

**录制操作**：
1. 鼠标点击某个虚函数格的 **[i]** 按钮，弹出教学说明，展示类定义伪代码：
   - 基类 `VirtualfuncTile` 的 `virtual int rent_price()`
   - 派生类重写后的 `rent_price()`（如纯虚函数、比例调整等）
2. 展示玩家踩中别人的虚函数格：
   - 情景 A：地主**没有**虚函数卡 → 按基类标准收租（或纯虚函数格无法收租）。
   - 情景 B：地主**有**虚函数卡 → 弹出对话框，地主选择"基类租金"或"派生类租金"。
3. 事件日志显示不同选择带来的不同金额。

**画外音**：
> "这是我们的核心创意之一——虚函数格。每个虚函数格都有基类和派生类两套定价逻辑，对应 C++ 的虚函数多态。地主持有虚函数卡时，可以像调用派生类重写函数一样，选择高收益的派生类租金；如果是纯虚函数格，没有卡就无法收租。"

**代码插入（闪切）**：
- `VirtualfuncTile::landOn` 代码（地主有卡时 emit promptVirtualFuncRent）。
- `VirtualfuncTile::calculateRent`（基类租金 vs 派生类租金计算差异）。

---

### 镜号 5：特色机制② — 迭代器格 & 迭代器卡（50s）

**画面**：聚焦 4 个迭代器格（三教、二教、理教、一教），展示传送机制。

**录制操作**：
1. 点击某个迭代器格的 **[i]** 按钮，展示 `vector<Tile>` 和 `auto it = location.begin();` 伪代码。
2. 玩家持有**迭代器卡**，踩中迭代器格 → 弹出选择：
   - 展示卡的子类型：Input（仅 ++）、Bidirectional（++/--）、Random Access（支持 +=2/-=2）
   - 玩家选择操作（如 ++），棋子瞬间传送到下一个迭代器格。
3. 若选择不被支持的操作（如 Input 卡选 --），展示"无效操作"提示。

**画外音**：
> "迭代器格模拟了 C++ STL 的迭代器机制。四个教学楼对应 vector 中的四个元素，玩家持有迭代器卡时可以在 vector 中按迭代器规则移动。Input、Bidirectional、Random Access 三种子类型，正好对应 STL 的迭代器分级。选错操作，就像代码里用 list 迭代器做随机访问一样——会编译失败，游戏里就是无效。"

**代码插入**：
- `IteratorTile::landOn`（检查 ITERATOR_CARD 并 emit promptIteratorCard）。
- `iteratorSubtypeSupports()` 函数（三种子类型的能力表）。

---

### 镜号 6：特色机制③ — 静态变量格 + 问答/上机课（50s）

**画面**：静态成员变量格 A/B/C、问答格、上机课格。

**录制操作**：
1. 点击静态成员变量格的 **[i]** 按钮，展示 `static int count` 和构造函数中 `count++` 的伪代码。
2. 玩家集齐 A/B/C 三块后，踩中其中一块建房，展示租金加成效果。
3. 玩家踩中**问答格**，弹出选择题界面（从 50 题题库中随机抽取）。
   - 答对后，65% 概率获得随机效果卡，展示获得卡牌的动画/提示。
4. 玩家踩中**上机课**（角格 14），弹出题目：
   - 说明文字："答对必得效果卡，但下回合跳过"
   - 答对后展示获得卡牌 + 玩家状态变为"跳过下一回合"。

**画外音**：
> "静态变量格三格集齐后触发加成，就像 C++ 中静态成员变量被同类所有对象共享。问答系统和上机课则嵌入了 50 道 C++ 面向对象真题，涵盖运算符重载、继承多态、模板、STL 和 C++11 新特性——答对了能拿效果卡，让游戏和课程复习真正结合起来。"

---

### 镜号 7：特色机制④ — 商店 & 效果卡系统（45s）

**画面**：麦叔的铺子（角格 7）、29 楼地下室（角格 21）、效果卡使用。

**录制操作**：
1. 玩家踩中**29 楼地下室**，弹出选择"是否进入商店" → 选择"进入"，棋子传送到麦叔的铺子。
2. 在商店界面展示 5 种卡牌：再丢一次、万能骰子、虚函数卡、跳过卡、迭代器卡。
3. 玩家购买一张**万能骰子**卡。
4. 下一回合，玩家掷骰前弹出是否使用万能骰子 → 选择使用，手动输入两个骰子点数（如 6+6）。
5. 展示玩家到达目标位置，连续对子获得额外回合。

**画外音**：
> "效果卡系统替代了传统的机会命运卡。商店设在麦叔的铺子，29 楼地下室可以传送过去。万能骰子让玩家像 DEBUG 一样自选点数，跳过卡能规避税收和他人租金——每张卡都对应一个策略决策点。"

---

### 镜号 8：代码架构讲解（90s）

**画面**：切换到 IDE/VS Code，展示源代码文件结构和关键代码片段。

**录制操作**（按以下顺序切换文件）：

**8.1 多态继承体系（20s）**
- 打开 `src/core/tile.h`
- 高亮 `class Tile` 基类，`virtual void landOn(...) = 0`
- 快速展开 11 个子类：`PropertyTile`、`VirtualfuncTile`、`StaticvalTile`、`IteratorTile`、`QATile`、`ShopTile`...
- 打开 `tile.cpp` 底部的 `createTile()` 工厂函数，说明 switch-case 创建不同地块。

**8.2 配置化棋盘（15s）**
- 打开 `src/core/config.h`
- 高亮 `BOARD_LAYOUT` 常量数组，28 个 `TileDef` 定义了整副棋盘。
- 说明：新增地块类型只需在枚举、工厂函数、config 数组中各加一行，扩展性极强。

**8.3 状态机驱动（25s）**
- 打开 `src/core/game.h`
- 高亮 `enum class GameState`：`WAITING_TO_START → PRE_ROLL → ROLLING → MOVING → LANDING → TURN_END`
- 展示 `Game` 类中的核心方法：`rollDice()`、`movePlayer()`、`handleLanding()`、`endTurn()`
- 打开 `game.cpp`，高亮 `onDiceRolled` 和 `handleLanding` 中的状态转换逻辑。

**8.4 信号/槽与 UI 解耦（20s）**
- 打开 `src/ui/mainwindow.cpp` 的 `connectSignals()`
- 展示 `Game` 发出的信号：`promptBuyProperty`、`promptVirtualFuncRent`、`promptIteratorCard`、`promptQA`...
- 展示 `MainWindow` 中的槽函数如何弹出模态对话框。
- 强调：核心逻辑层（core）完全不依赖 Qt GUI，仅通过信号与 UI 层通信。

**8.5 效果卡与迭代器子类型（10s）**
- 打开 `src/core/effectcard.h`
- 高亮 `enum class EffectCardType`、`IteratorSubtype`、`IteratorOp`
- 快速展示 `iteratorSubtypeSupports()` 能力查询函数。

**画外音**：
> "架构上我们严格分层：core 层负责游戏逻辑和状态机，ui 层负责 Qt Widgets 渲染和交互。11 种地块通过多态统一接口，棋盘布局完全配置化。信号/槽机制让逻辑和界面解耦，新增一种地块或卡牌类型只需扩展枚举、工厂函数和 config 数组，代码可维护性和可扩展性都很强。"

---

### 镜号 9：收尾 & 亮点总结（30s）

**画面**：回到游戏界面，展示一局游戏接近尾声的状态（多名玩家资金变动、地产分布、房屋密集）。

**录制操作**：
1. 快速切换几个玩家的回合，展示激烈的地产争夺和租金往来。
2. 一名玩家破产，事件日志显示破产清算信息。
3. 最后展示游戏结束画面（若有），或直接切到标题画面。

**画外音/字幕**：
> "《程设大富翁》把 C++ 的虚函数、迭代器、静态成员变量、运算符重载等抽象概念，变成了可玩、可交互、可策略化的游戏机制。我们希望它不只是一个大富翁游戏，更是一个能帮助同学们在娱乐中巩固 OOP 知识的教学工具。"

**结尾画面**：
- 项目 GitHub 仓库 / 二维码
- 团队成员名字
- "谢谢观看！"

---

## 录制技术建议

### 推荐参数
- **分辨率**：1920×1080（游戏窗口可固定 960×800 置于画面中央）
- **帧率**：30fps 足够，棋盘动画本身不快
- **音频**：画外音清晰，游戏音效（如果有）适当保留

### DEBUG 模式妙用
在录制中为了精准展示特定机制，强烈建议开启 **DEBUG 模式**（菜单 → DEBUG 模式）：
- 虚函数格演示：手动输入点数让玩家精准踩中目标格
- 迭代器卡演示：提前通过 DEBUG 骰子让玩家持有卡片并到达迭代器格
- 破产/胜利演示：通过连续操作快速触发结局

### 字幕标注建议
在以下画面建议叠加文字标注：
- 虚函数格选择时：标注"基类租金 ¥XXX" vs "派生类租金 ¥XXX"
- 迭代器卡选择时：标注当前卡类型（如"Random Access 迭代器卡"）
- 代码展示时：标注文件名和行号范围

### 备选精简版（若时间限制 3 分钟）
若路演只有 3 分钟，保留以下镜号：
1. 开场（20s）→ 2. UI 概览（20s）→ 4. 虚函数格（45s）→ 5. 迭代器格（40s）→ 8. 代码架构（60s）→ 9. 收尾（15s）
跳过问答系统和商店系统的单独演示，在虚函数/迭代器演示中一笔带过即可。
